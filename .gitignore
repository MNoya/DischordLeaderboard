# Secrets / local-only files
/legacy/user_ids.py
/legacy/credentials.py
.env
.env.*
!.env.example
# Frontend's production env contains only the Supabase URL + publishable key,
# which are by design public (they end up inlined into the client bundle anyway).
!frontend/.env.production

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# IDE
.idea/
.vscode/

# Logs
logs/

# Local 17lands response cache for dev iteration
cache/

# Claude Code: user-specific settings and transient agent state
.claude/settings.local.json
.claude/worktrees/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
