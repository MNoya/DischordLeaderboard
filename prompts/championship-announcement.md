# Set Championship announcement (paste-ready)

The full season-closer promo, posted manually for now. Fill the `{...}` placeholders, refine the opener,
and paste into the coordination channel. Plain text so it reads the same from any author.

Placeholders:
- `{set_code}` — active set code, e.g. `SOS`
- `{set_name}` — full set name, e.g. `Secrets of Strixhaven`
- `{channel}` — coordination channel mention
- `{next_name}` — the set that arrives next
- `<t:{unix}:R>` — Discord relative timestamp for the next set's release
- Links wrapped in `<>` so Discord doesn't expand them into preview embeds
- Standings link is set-scoped (`/leaderboard/{set_code}`) so it still points here after the board rotates

---

👑 **{set_code} Set Championship**

Final week of {set_name}. The leaderboard decides who plays for the championship.

**How it works**
• The eight highest-ranked players who show up take the seats.
• RSVP on the championship event in {channel} if you can make it. Seats go to the highest standings among those who show, the rest are alternates for no-shows.
• Best of 3, Swiss, three rounds, one Champion.

📊 Standings: <https://dischord.pages.dev/leaderboard/{set_code}>
📺 Streamed live: <https://twitch.tv/GatoDelFuego>

Regular pods are paused this week. **{next_name}** arrives <t:{unix}:R>.

---

The opener ("Final week of {set_name}…") is provisional — reword before a real run.
