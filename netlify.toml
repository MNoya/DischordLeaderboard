[build]
  base = "frontend"
  command = "npm install && npm run build"
  publish = "frontend/dist"
  # Skip rebuilds when neither the frontend source nor the build config
  # changed in this commit
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- frontend/ wrangler.jsonc netlify.toml"
